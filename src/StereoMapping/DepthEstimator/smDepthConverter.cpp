#include "../../../include/StereoMapping/DepthEstimator/smDepthConverter.h"

namespace StereoMapping {
	void DepthConverter::smIdealBinocularDisparityToDepth(f64* disparityMap, f64* depthMap, u32 imageWidth, u32 imageHeight, u32 cameraBaseline, u32 cameraFocalLength) {
		for (i32 i = 0; i < imageWidth; i++) {
			for (i32 j = 0; j < imageHeight; j++) {

			}
		}
	}
}